language: python
python:
  - "pypy"
  - "2.7"
  - "3.3"
install:
    - pip install twisted -r test-requirements.txt
    - python setup.py --quiet install
script:
    - trial minion
    - python -m doctest README.rst
